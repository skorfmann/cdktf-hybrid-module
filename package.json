{
  "name": "root",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "lerna run build"
  },
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}